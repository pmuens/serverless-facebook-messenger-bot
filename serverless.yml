service: serverless-facebook-messenger-bot

provider:
  name: aws
  runtime: nodejs4.3

functions:
  webhook:
    handler: handler.webhook
    events:
      - http:
          method: GET
          path: webhook
          integration: lambda
      - http:
          method: POST
          path: webhook
          integration: lambda
